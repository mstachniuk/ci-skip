on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: A job to say hello
    steps:
      - uses: actions/checkout@v2
      - id: foo
        uses: mstachniuk/ci-skip@master
        with:
          exit-code: 11
          fail-fast: true
